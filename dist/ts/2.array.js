let arr = [1, 2, 3];
let str = ['a', 'b', 'c'];
let _arr = [1, 2, 3];
let multi = ['hello', 10, false];
multi = [10, 'hi'];
// 자리는 상관이 없당. 그리고 빠지는것도 상관이 없다.
// 자리를 정할수 있을까? 빼먹으면 안되는거 아닌가? ->tuple type 튜블!
let tuple = [1, 2];
// tupleA = ['a']; 에러!
// tupleA = ['a',64]; 에러!
let person = ['py', 10];
const user = [
    ['윤경', 10],
    ['윤겅', 10],
    ['운경', 10]
];
export {};
